#include <stdio.h>
 
int main() {
 
    /**
     * Escreva a sua solução aqui
     * Code your solution here
     * Escriba su solución aquí
     */
     double A;
     double B;
     double C;
     double SOMA;
     scanf("%lf", &A);
     scanf("%lf", &B);
     scanf("%lf", &C);
     SOMA = (A*2) + (B*3) + (C*5);
     printf("MEDIA = %.1lf\n", SOMA/10);
    return 0;
}