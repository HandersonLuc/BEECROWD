#include <stdio.h>
 
int main() {
 
    /**
     * Escreva a sua solução aqui
     * Code your solution here
     * Escriba su solución aquí
     */
     double A;
     double B;
     double SOMA;
     scanf("%lf", &A);
     scanf("%lf", &B);
     SOMA = (A*3.5) + (B*7.5);
     printf("MEDIA = %.5lf\n", SOMA/11);
    return 0;
}